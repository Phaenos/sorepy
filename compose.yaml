version: '3.8'

services:
  sorepy:
    image: phaenos/sorepy
    ports:
      - "5000:5000"
    volumes:
      - /sorepy/data:/sorepy/data
    networks:
      - default
    restart: unless-stopped

networks:
  default:
    driver: bridge

